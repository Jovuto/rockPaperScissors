function getComputerChoice() {
    // create a random number 1 to 3
    let choiceNumber = Math.ceil((Math.random()) * 3);
    console.log(choiceNumber);
    // translate that number into rock, paper, or scissors
    // return the output
}